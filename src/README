Execution instructions:

See:
 ./client --help
 ./server --help
 ./node --help

Only one node should run per machine. If you want to run multiple nodes you 
must move into a new folder and run from there.

node.py
gridservice/*
gridservice/node/*

Demo scripts have been included to demonstrate functionality of algorithms. See:
demos/README
